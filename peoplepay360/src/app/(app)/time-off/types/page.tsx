import { ComingInPhase } from "@/components/shared/ComingInPhase"

export const metadata = { title: "Time Off Types — PeoplePay360" }

// P5 replaces this stub.
export default function TimeOffTypesPage() {
  return (
    <ComingInPhase
      title="Time Off Types"
      phase="P5"
      hours="10–13"
      description="Leave policy configuration — unit (days or hours), whether an allocation is required, the approval mode, and whether the type is paid. These are policy rules, not employee transactions."
    />
  )
}
