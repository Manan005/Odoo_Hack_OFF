import { ComingInPhase } from "@/components/shared/ComingInPhase"

export const metadata = { title: "Time Off Allocations — PeoplePay360" }

// P5 replaces this stub.
export default function TimeOffAllocationsPage() {
  return (
    <ComingInPhase
      title="Time Off Allocations"
      phase="P5"
      hours="10–13"
      description="Grants of leave balance per employee and type, showing Allocated, Taken and Remaining. Only an approved allocation creates available balance."
    />
  )
}
